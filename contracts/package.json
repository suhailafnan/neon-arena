{
    "name": "neon-arena-contracts",
    "version": "1.0.0",
    "scripts": {
        "compile": "hardhat compile",
        "deploy:moonbase": "hardhat run polkadot/deploy.js --network moonbase",
        "verify": "hardhat verify"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "hardhat": "^2.19.0",
        "dotenv": "^16.3.1"
    }
}